import React from 'react';
import Grid from "@mui/material/Grid";

const HomePage = () => {
    return (
        <Grid container>
            <Grid item>
            </Grid>
        </Grid>
    );
};

export default HomePage;